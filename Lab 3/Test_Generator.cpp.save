#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(int argc, char* argv[]) {
    srand(time(NULL));
    srand(rand());
    int len = atoi(argv[1]);
    int max_num = 0;
    max_num = atoi(argv[2]);
    int* arr = (int*) calloc(len, sizeof(int));
    for (int i = 0; i < len; i++) {
        arr[i] = rand();
        printf("%d\n", arr[i]);
    }
}
